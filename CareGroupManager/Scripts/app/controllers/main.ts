module Controllers {
    class MainCtrl {
        public static $inject = [];

        constructor() {
            
        }
    }
    
    angular.module('care-group')
        .controller('mainCtrl', MainCtrl);
}